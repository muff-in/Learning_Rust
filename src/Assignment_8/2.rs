// Explicit Definition

fn main() {
    let explicit_variable: i32 = 10; // this is how we define explicitly typed variables

    // let variable_name: data_type = value;

    println!("{}", explicit_variable);
}
