// Named Arguments

// Named arguments are used to specify the names of the values in a sentence.

fn main() {
    println!(
        "{langauge} is a {kind} programming language.",
        language = "Rust",
        kind = "statically-typed"
    );
}

// output: Rust is a statically-typed programming language.
